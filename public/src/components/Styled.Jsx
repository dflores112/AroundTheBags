import styled from 'styled-components';

const PlayerContainer = styled.div`
    border: 3px solid;
    border-color: Black;
    width:300px;
`;

const styles = {
  PlayerContainer,
};

export { styles as default };
